{
  "name": "selenium-java-training",
  "version": "1.0.0",
  "description": "Interactive Selenium with Java learning platform with AI assistant",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js",
    "vercel-build": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
